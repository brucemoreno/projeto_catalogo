<%= form_with(model: log) do |form| %>
  <% if log.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(log.errors.count, "error") %> prohibited this log from being saved:</h2>

      <ul>
        <% log.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :data_de_aprovacao, style: "display: block" %>
    <%= form.date_field :data_de_aprovacao %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
